name: Build and Deploy to EC2

on:
  push:
    branches: [ master ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v3

      - name: ☕ Set up Java 19
        uses: actions/setup-java@v3
        with:
          java-version: '19'
          distribution: 'temurin'

      - name: ✅ Give execute permission to mvnw
        run: chmod +x mvnw

      - name: ⚙️ Build Spring Boot Project (skip tests)
        run: ./mvnw clean package -DskipTests

      - name: 📤 Upload JAR to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          source: target/Room_Management_System-0.0.1-SNAPSHOT.jar
          target: /home/ec2-user/

      - name: Deploy and Run Spring Boot JAR on EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          timeout: 300s
          command_timeout: 20m
          script: |
            echo "=== Starting New Deployment ==="
            echo "Timestamp: $(date)"
            
            echo "=== Verifying File Transfer ==="
            ls -la /home/ec2-user/
            
            # Determine JAR path
            if [ -f "/home/ec2-user/target/Room_Management_System-0.0.1-SNAPSHOT.jar" ]; then
              JAR_PATH="/home/ec2-user/target/Room_Management_System-0.0.1-SNAPSHOT.jar"
            elif [ -f "/home/ec2-user/Room_Management_System/target/Room_Management_System-0.0.1-SNAPSHOT.jar" ]; then
              JAR_PATH="/home/ec2-user/Room_Management_System/target/Room_Management_System-0.0.1-SNAPSHOT.jar"
            else
              echo "❌ JAR file not found!"
              exit 1
            fi
            echo "✅ JAR found at $JAR_PATH"

            echo "=== Setting Environment Variables ==="
            export MONGO_URI="${{ secrets.MONGO_URI }}"
            export MAIL_USERNAME="${{ secrets.MAIL_USERNAME }}"
            export MAIL_PASSWORD="${{ secrets.MAIL_PASSWORD }}"
            export AWS_ACCESS_KEY_ID="${{ secrets.AWS_ACCESS_KEY_ID }}"
            export AWS_SECRET_ACCESS_KEY="${{ secrets.AWS_SECRET_ACCESS_KEY }}"
            export AWS_REGION="${{ secrets.AWS_REGION }}"
            export AWS_S3_BUCKET="${{ secrets.AWS_S3_BUCKET }}"
            
            # Set frontend.base-url
            export FRONTEND_URL="${{ secrets.FRONTEND_BASE_URL }}"
            echo "FRONTEND_URL set to FRONTEND_URL"
            
            echo "=== Starting Application ==="
            # Clear previous logs
            > output.log

            # Start Spring Boot with extra system property for frontend.base-url
            nohup java -Dfrontend.base-url="$FRONTEND_BASE_URL" -jar "$JAR_PATH" > output.log 2>&1 &
            JAVA_PID=$!
            echo $JAVA_PID > app.pid
            echo "Application started with PID: $JAVA_PID"

            # Wait for app to start
            for i in {1..90}; do
              if kill -0 $JAVA_PID 2>/dev/null; then
                if grep -q "Started RoomManagementSystemApplication" output.log; then
                  echo "✅ Application started successfully!"
                  tail -20 output.log
                  exit 0
                elif grep -q "ERROR\|Exception\|Failed\|Application run failed" output.log; then
                  echo "❌ Application startup errors detected"
                  tail -50 output.log
                  exit 1
                fi
              else
                echo "❌ Process died unexpectedly"
                cat output.log
                exit 1
              fi
              sleep 2
            done

            echo "⚠️ Application startup timeout - process may still be running"
            tail -50 output.log
